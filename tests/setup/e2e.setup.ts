import { beforeAll, afterAll } from 'vitest'

// Global setup
beforeAll(async () => {
	// Any global setup needed before all E2E tests
})

afterAll(async () => {
	// Any global cleanup needed after all E2E tests
})
